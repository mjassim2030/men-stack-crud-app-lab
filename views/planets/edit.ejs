<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create a Planet</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />

</head>

<body>

  <h1>Edit <%= planet.name %>!</h1>
  <form class="form" action="/planets?id=<%= planet._id %>&_method=PUT" method="POST">

    <input type="text" name="name" id="name" placeholder="Name" value="<%= planet.name %>" />
    <textarea  name="description" id="description" placeholder="Description"><%= planet.description %></textarea>
    <div class="checkbox-div">
      <label for="isHabitable">Habitable?</label>
      <input type="checkbox" name="isHabitable" id="isHabitable" <% if(planet.isHabitable) { %>checked <% } %> />
    </div>

      <button type="submit">Update Data</button>
  </form>

  <div class="planet-actions">
    <a class="planets-index-link" href="/">
      <div class="index-link-text">🏠 Home</div>
    </a>
    <a class="planets-index-link" href="/planets">
      <div class="index-link-text">Back to Planetify Universe</div>
    </a>
  </div>
</body>

</html>